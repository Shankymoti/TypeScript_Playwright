
var , let or const
var age =40
age+=10
console.log(age)

age="forty"
age -=10

Typescript is a superset of javascript which has static typing

Typescript - Synctatic superset
adding basic syntax of javascript
Add - Data types
      OOps concepts
      Methods, constructor, inheritance
      arrow functions

Typesscript uses compiler for type checking
Typescript is transpiled into javascript using the typescript compiler


npm install typescript --save--dev
npx tsc


Boolean - true/false
number  - it will support whole number and floating point number
string - text value , char value

symbol - unique identifier value 
bigint - larger positive and negative number

any - it is base for all datatypes, it can be initialized with any values


let firstnaame : string = "Tom";//explicit initialization

let firstname="Tom";//implicit type

let firstname:string;// here it will work fine
firstname=33;

let firstname="TOM"; Implicitly some default value has to be initialized
firstname=33;  // js will not throw any error for mismatch datatype but ts 


let firstname:any;
any will disable type checking  



------------------------------------------------------------------------------------------------------------------------------

Typescript supports only single and multilevel inheritance and uses extends keyword

---------------------------------------------------------------------------------------------------------------------------------
Abstract class and interfaces

suppose we have some methods that is common and want to implements then we use interfaces

Inheritance is IS-A_Relationship and Interface is HAS-A-Relationship
------------------In java-----------------------------------------
Abstract Class 

House is a type of building
Villa is a types of building
Flat is a type of building

abstract calculateSqFeet()


Interface : 
fly():

kite
plane
bird
superman


unrelated classes share some common behabiour then we go for interfaces.

-------In typescript--------------------------------------
In ts interfaces allows to define a structure or shape of an object. They are defined with data member and nethods, class which implements the interface has to follow the same structure. it is neither is a relationship nor has a relationship.

and with the help of data members and methods which ever class is implementing interface,they should follow the same structure.


UI -Entity, BAO, DAO
  interface IShoppingCartDAO{
int Register()
List<T> Display();
}

class CustomerDAO implements IShoppingCartDAO
{
int Register()  -  register new customer
}

class CategoryDAO implements IShopingCartDAO
{
int Register() -  register new category
}


best example is collection like .add() methods in list and set.






var , let or const.txt
Displaying var , let or const.txt.
----------------------------------------------------------------------------------------------------
Required ;
Check in command prompt
node -v
npm -v
npm install -g typescript
tsc -v  // it is used to check ts version and tsc is used to compile from ts to js
cd/
mkdir TypeScriptExamples
cd TypeScriptExamples
npm init -y // intialize package.json
npm install typescript --save-dev
npx tsc --init // create tsconfig.json
tsc -v
or
vsCode typescript compiler


design - HTML and to validate the HTML and make the pages dynamic or interactive we need Javascript
typescript - enhanced version of Javascript

typescript is by Microsoft
Add static typing, classes, interfaces,collection advanced features ontop of Javascript
typescript is scripting language for Angular Application
typescript will have extension .ts

typescript can be compiled. On compilation(Transpiled) into plain Javascript
because the browser or node environment can recognize Javascript

typescript Benefitsg
1. Type Safty : catch errors at compile time not at runtime

let age = 40
console.log(age);
age=age+"five"
console.log(age)
// o/p. 40five

NAN - if calculation can not happen it prints o/p as NAN
let age = 40;
age=age+"five"// Error at compile time


Javascript supports - classes,constructor, methods Access Specifier - (All class variables are public). In case of Javascript if we want to create private variables then we have specify hash and variableName
#variableName

class Accounts{
    #balance=0; // here it is private
    constructor(accno,name, status,bal)//accno, name and status are by default public
    {
        this.accno=accno;
        this.name = name;
        this.status=status;
        this.#balance=bal;
    }

    var obj= new Accounts(102,"Deepa","Active",6000);
    obj.name="Anu";
    obj.#balance=2000;//Error

    //Javascript
    function add(a,b){
        return a+b;
    }

    console.log("10",5);// it will not be a errors
    // o/p 105

    // typescript

    function add(a:number, b:number):number
    {
        return (a+b)
    }
    console.log("5",10)// Error
}

static typing(string, Boolen, number, any, undefined, null)
interfaces
classes and Inheritance
Generics
Enums
Type inferences
Async/Await support
Decorators

